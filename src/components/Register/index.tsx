import React from 'react';
import Layout from 'Layout';
import { FormContainer } from 'pages/Register/containers';

function Register() {
  return (
    <Layout isWrap>
      <FormContainer />
    </Layout>
  );
}

export default Register;
